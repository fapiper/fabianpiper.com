apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DNS records are now managed by Terraform (modules/dns).
# external-dns has been removed to avoid conflicts with Terraform-owned records.
# ArgoCD (prune: true) will delete the previously deployed resources on next sync.
resources: []
