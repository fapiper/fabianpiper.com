apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: oci-vault
  annotations:
    argocd.argoproj.io/sync-wave: "15"
spec:
  provider:
    oracle:
      vault: "" # populated by cloud-init
      region: "eu-frankfurt-1"
      principalType: InstancePrincipal
