import:
  - mixins/stage/prod
  - mixins/region/fra

vars:
  tenant: "glg"

components:
  terraform:
    vault:
      vars: {}

    k3s-cluster:
      vars:
        common_tags:
          Environment: "prod"

    oidc:
      vars: {}

