import:
  - mixins/stage/prod
  - mixins/region/fra

vars:
  tenant: "glg"

components:
  terraform:
    cluster:
      vars:
        availability_domain: 1
        common_tags:
          Environment: "prod"