components:
  terraform:
    vault:
      metadata:
        component: vault
      vars:
        enabled: true
        name: vault
        git_username: "{{ env.TF_VAR_git_username }}"
        git_pat: "{{ env.TF_VAR_git_pat }}"
        mixpanel_token: "{{ env.TF_VAR_mixpanel_token }}"
