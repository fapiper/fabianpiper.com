components:
  terraform:
    k3s-cluster:
      metadata:
        component: k3s-cluster
      vars:
        enabled: true
        name: "k3s-cluster"
        vcn_id: "{{ (atmos.Component \"networking\" .stack).outputs.vcn_id }}"
        public_subnet_id: "{{ (atmos.Component \"networking\" .stack).outputs.subnet_id }}"