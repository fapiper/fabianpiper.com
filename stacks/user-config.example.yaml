# User-Specific Configuration
# Copy this file to stacks/user-config.yaml and customize
# Only contains values that are USER-SPECIFIC (not infrastructure defaults)

vars:
  # === Git Configuration (USER-SPECIFIC) ===
  # Your GitHub username and repository
  git:
    username: "fapiper"
    repository: "https://github.com/fapiper/fabianpiper.com.git"

  # === SSH Configuration (USER-SPECIFIC) ===
  # Path to YOUR SSH public key
  ssh:
    public_key_path: "C:\\Users\\fabia\\.ssh\\root@glg\\id_rsa.pub"

  # === Domain Configuration (USER-SPECIFIC) ===
  # Your domain for the deployment
  domain:
    name: "fabianpiper.com"
    subdomain: "glg"

